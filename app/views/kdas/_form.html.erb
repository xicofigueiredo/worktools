<%= form_with(model: kda, local: true) do |form| %>
  <% if kda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kda.errors.count, "error") %> prohibited this kda from being saved:</h2>

      <ul>
      <% kda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.date_field :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_field :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
