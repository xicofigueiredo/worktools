<!-- app/views/goals/index.html.erb -->
<h1>Sprint Goals</h1>

<ul>
  <% @goals.where(type: Goal.types[:sprint]).each do |goal| %>
    <li><%= link_to goal, goal %></li>
  <% end %>
</ul>

<%= link_to 'New sprint Goal', new_sprint_goals_path %>
