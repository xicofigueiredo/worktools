<!-- app/views/goals/index.html.erb -->
<h1>Weekly Goals</h1>

<ul>
  <% @goals.where(type: Goal.types[:weekly]).each do |goal| %>
    <li><%= link_to goal, goal %></li>
  <% end %>
</ul>

<%= link_to 'New Weekly Goal', new_weekly_goals_path %>
