<%# <%= form_with url: new_weekly_meeting_path, method: :get, local: true, class: "mb-3" do |form| %>
  <%# <%= form.select :week_id, %>
                  <%# Week.all.collect { |w| [w.name, w.id] },
                  { include_blank: true },
                  { onchange: 'this.form.submit();', class: "form-select d-inline-block w-auto" } %>
  <%# <%= form.submit "New Weekly Meetings Schedule", class: "btn btn-primary" %>
<%# <% end %>

<%# <% @weekly_meetings.each do |weekly_meeting| %>
  <%# <div class="card mb-3"> %>
    <%# <div class="card-body">
      <p class="card-text"> %>
        <%# <strong><%= weekly_meeting.week.name</strong><br>  %>
        <%# <strong>Start Date:</strong> <%= weekly_meeting.week.start_date <br> %>
        <%# <strong>End Date:</strong> <%= weekly_meeting.week.end_date %>
      <%# </p> %>
      <%# <%= link_to "Show", weekly_meeting_path(weekly_meeting), class: "btn btn-primary mr-2" %>
      <%# <%= link_to "Edit", edit_weekly_meeting_path(weekly_meeting), class: "btn btn-secondary mr-2" %>
      <%# <%= link_to "Delete", weekly_meeting_path(weekly_meeting), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger", style: "color: white;" %>
    <%# </div> %>
  <%# </div> %>
<%# <% end %>
<%# <h2>Weekly Meetings cooming soon...</h2> %>
<div class="container py-4">
  <div class="card shadow-sm border-0 mx-auto" style="max-width: 1000px;">
    <div class="card-body text-center p-4">
      <h4 class="card-title mb-3 text-muted">
        <i class="bi bi-calendar-event me-2"></i>Exam Season Information
      </h4>
      <p class="text-secondary mb-4">View the exam registration rules and important dates below</p>
      <%= link_to "https://edubgabravegeneration-my.sharepoint.com/:x:/g/personal/princess_carmela_bravegenerationacademy_com/IQALwQTlHHeBSbk4_eng4EPaAZnVPMhRdnQSPxVUqdBSsP4?e=c9dtA4",
         class: "btn btn-primary btn-lg px-4 mb-4",
         target: "_blank",
         rel: "noopener noreferrer" do %>
        <i class="bi bi-file-earmark-text me-2"></i>More info about exam seasons
      <% end %>
    </div>
    <div class="card-img-bottom">
      <%= image_tag "rules.png", class: "img-fluid rounded-bottom", alt: "Exam registration rules" %>
    </div>
  </div>
</div>
