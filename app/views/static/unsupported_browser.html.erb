<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Unsupported Browser</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .container {
      flex: 1; /* Ensures the content takes up available space */
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .card {
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      padding: 40px; /* Increased padding for a larger card */
      max-width: 600px; /* Slightly wider */
      width: 90%;
      text-align: center;
    }

    .card h1 {
      color: #333;
      margin-bottom: 20px;
      font-size: 2rem; /* Larger font for the title */
    }

    .card p {
      color: #555;
      line-height: 1.6;
      margin-bottom: 30px;
      font-size: 1.1rem; /* Slightly larger text */
    }

    .card a {
      display: inline-block;
      padding: 15px 25px; /* Larger button */
      background-color: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      font-size: 1rem;
    }

    .card a:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <!-- Card Content -->
  <div class="container">
    <div class="card">
      <h1>Unsupported Browser</h1>
      <p>
        We noticed you're not using Google Chrome. For the best experience, please switch to Google Chrome to avoid data loss and ensure everything works smoothly.
      </p>
      <a href="https://www.google.com/chrome/" target="_blank">Download Google Chrome</a>
    </div>
  </div>
</body>
</html>
